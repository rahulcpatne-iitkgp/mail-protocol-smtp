# mail-protocol-smtp
A command-line email system implemented in C using TCP sockets. The project includes a SimpleMail server (smserver) for managing user mailboxes and a client (smclient) for sending and retrieving emails, built on custom text-based protocols (SMTP2 and SMP).
